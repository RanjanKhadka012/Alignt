# Copy to .env.local and restart Vite. Server-side only; never use VITE_ for keys.
# Create your key at https://ollama.com/settings/keys
OLLAMA_API_KEY=
# Set a model available to your Ollama Cloud account.
OLLAMA_MODEL=gpt-oss:120b
